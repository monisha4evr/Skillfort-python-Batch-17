To create Project:
******************
python -m venv env_folder_name 
env_folder_name\Scripts\activate 
pip install django
django-admin startproject projectname 
cd projectname 
python manage.py startapp appname 
python manage.py runserver 


Ctlr+c to cut server

After Creation 
env_folder_name\Scripts\activate
cd projectname
python manage.py runserver


Database: (Local Database)
python mange.py makemigrations
python manage.py migrate

python manage.py runserver


to create admin user 

python manage.py createsuperuser
username:admin
email 
mobile 
password  admin123
conf-password admin123
y/n => y

************************************************************************

To connect external database:

Step 1: 
create Database in mysql

Step 2: 
project -> settings.py 
Database 
'default': {
                'ENGINE': 'django.db.backends.mysql',
                'NAME': 'lighthouse',
                'USER':'root',
                'PASSWORD':'moni@123',
                'PORT':3306,
                'HOST':'localhost'
            }

Step 3: 
ctrl+c 
pip install mysqlclient

Step 4:
python manage.py runserver 

step 5:
ctrl+c 
python manage.py makemigrations
python manage.py migrate

Create Useruser again

********************************************
To create more than one app 
python manage.py startapp appname 


Generic Views
-------------
Generic View:

Generic Views are Django's built-in views. They reduce the amount of code we write.

Generic Views
      │
      ├── Display Generic Views
      │      │
      │      ├── TemplateView (Display a static HTML page)
      │      ├── ListView (Display multiple records from a model)
      │      └── DetailView (Display a single record based on its ID or slug)
      │
      └── Editing Generic Views
             │
             ├── FormView (Handle a normal Django form (not necessarily tied to a model))
             ├── CreateView (Insert/Create a new record)
             ├── UpdateView (Update an existing record)
             └── DeleteView (Delete a record)

from django.views.generic import TemplateView,ListView,DetailView


Filter:
------
# Ignore case
StudentDetails.objects.filter(course__iexact="python")

# Contains
StudentDetails.objects.filter(course__contains="Py")

# Ignore case + Contains
StudentDetails.objects.filter(course__icontains="py")

# Starts with
StudentDetails.objects.filter(course__startswith="P")

# Ends with
StudentDetails.objects.filter(course__endswith="on")

# Greater than
StudentDetails.objects.filter(fees_paid__gt=5000)

# Less than
StudentDetails.objects.filter(fees_paid__lt=5000)

# Greater than or equal
StudentDetails.objects.filter(fees_paid__gte=5000)

# Less than or equal
StudentDetails.objects.filter(fees_paid__lte=5000)



Step 1:
    pip install djangorestframework
    

Step 2:

settings.py:
INSTALLED_APPS = [
    ...
    'rest_framework',
]
Note: pip freeze > requirements.txt